variables:
  SONAR_TOKEN: sqp_c180fbdad2fc5433fd2dbed5ca31c2f753ed6c44
  SONAR_HOST_URL: http://localhost:9000/sonar
stages:
  - sast
  - sca
  - secrets
  - build
  - container
  - custom_dast

sonarqube-check:
  image: 
    name: sonarsource/sonar-scanner-cli:latest
    entrypoint: [""]
  stage: sast
  variables:
    SONAR_USER_HOME: "${CI_PROJECT_DIR}/.sonar"  # Defines the location of the analysis task cache
    GIT_DEPTH: "0"  # Tells git to fetch all the branches of the project, required by the analysis task
  cache:
    key: "${CI_JOB_NAME}"
    paths:
      - .sonar/cache
  script: 
    - sonar-scanner
  allow_failure: true
  only:
    - master # or the name of your main branch

semgrep:
  image: returntocorp/semgrep-agent:v1
  stage: sast
  script: semgrep-agent
  allow_failure: true
  variables:
    SEMGREP_RULES: >-
      p/security-audit
      p/secrets
      p/ci
      p/python
